# App
DEBUG=false

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=kospi200
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# JWT
JWT_SECRET=
JWT_ACCESS_EXPIRE_MINUTES=60
JWT_REFRESH_EXPIRE_DAYS=7

# Pipeline
PIPELINE_CONFIG_PATH=
PIPELINE_BASE_DIR=
PIPELINE_PROJECT_ROOT=
TICKER_NAME_MAPPING_PATH=ticker_name_mapping.json

# Scheduler
SCHEDULER_ENABLED=true
SCHEDULER_HOUR=6
SCHEDULER_MINUTE=0

# Rate Limiting
RATE_LIMIT=60/minute
