# 🚀 4-에이전트 팀 파이프라인 — Claude Code 실행 가이드

> **환경**: Claude Code (Git Bash / Terminal)
> **목표**: 기존 프로젝트 코드 분석 → 사용자 검토 → 웹 서비스 완성
> **원칙**: 단계별 실행, 토큰 절약, 중단 시 재개 가능

---

## 📋 전체 파이프라인 요약

```
[STAGE 0] 환경 세팅 & 압축 해제
[STAGE 1] 코드 구조 분석 (에이전트: Planner + Worker)
[STAGE 2] 분석 보고서 생성 → 사용자 검토
[STAGE 3] 수정사항 반영 & 설계 확정
[STAGE 4] 백엔드 구축 (에이전트: Worker)
[STAGE 5] 프론트엔드 구축 (에이전트: Worker)
[STAGE 6] 통합 & 테스트 (에이전트: Critic)
[STAGE 7] 배포 준비 & 최종 점검
```

**중단 복구 규칙**: 각 STAGE 완료 시 `STAGE_N_COMPLETE.md` 체크포인트 파일 생성.
재개 시 가장 최근 체크포인트부터 시작.

---

## 🔧 사전 준비

프로젝트 폴더 안에 기존 코드 압축 파일을 넣어두세요.

```
your-project/
├── source-code.zip (또는 .tar.gz)  ← 기존 프로젝트 코드
└── (이후 에이전트가 생성하는 파일들)
```

---

## STAGE 0: 환경 세팅 & 압축 해제

### 프롬프트 0-1: 프로젝트 초기화

```
너는 프로젝트 분석 및 웹서비스 구축을 위한 오케스트레이터 에이전트다.
아래 규칙을 따라라.

[규칙]
1. 현재 디렉토리의 압축 파일을 찾아 해제하라.
2. 해제된 프로젝트 루트의 디렉토리 트리를 2단계 깊이로 출력하라.
3. 아래 형식으로 결과를 정리하라.
4. 불필요한 설명 금지. 결과만 출력.

[출력 형식]
## 프로젝트 초기 정보
- 압축파일명: 
- 해제 경로: 
- 디렉토리 구조:
```
(트리 출력)
```
- 주요 파일 수: N개
- 감지된 언어/프레임워크: 
- package.json 존재 여부: Y/N
- requirements.txt 존재 여부: Y/N

## 다음 단계
STAGE 1 진행 준비 완료. "1단계 시작"을 입력하세요.
```

---

## STAGE 1: 코드 구조 분석

### 프롬프트 1-1: 구조 스캔 (Planner 역할)

```
너는 코드 분석 Planner다. 아래 프로젝트를 분석 계획을 세워라.

[프로젝트 경로]
{{PROJECT_PATH}}

[규칙]
1. 아래 항목별로 분석 대상 파일을 식별하라.
2. 각 항목당 핵심 파일 최대 5개만 선정.
3. JSON으로 출력. 설명 금지.

[분석 항목]
A. 진입점 (엔트리포인트, main, index, app)
B. 라우팅/API 엔드포인트 정의
C. 데이터 모델/스키마 (DB, ORM, 타입 정의)
D. 비즈니스 로직 (서비스, 컨트롤러, 유틸)
E. 프론트엔드 구조 (컴포넌트, 페이지, 상태관리)
F. 설정 파일 (환경변수, 빌드, 배포)
G. 테스트

[출력 형식]
{"analysis_plan":[
  {"category":"A","label":"진입점","files":["path1","path2"]},
  ...
]}
```

### 프롬프트 1-2: 파일별 심층 분석 (Worker 역할)

> ⚠️ 토큰 절약: 한 카테고리씩 실행. 전체를 한번에 하지 말 것.

```
너는 코드 분석 Worker다.
아래 파일들을 읽고 분석 결과를 반환하라.

[카테고리] {{CATEGORY_LABEL}}
[파일 목록]
{{FILE_LIST}}

[규칙]
1. 각 파일을 읽고 아래 항목을 추출.
2. 파일당 5줄 이내로 요약.
3. 코드 전체를 복사하지 마라. 핵심 패턴만 기술.

[추출 항목]
- 역할: 이 파일이 하는 일 (1줄)
- 의존성: import/require 대상 (내부 모듈만)
- 패턴: 사용된 설계 패턴 (MVC, REST, hooks 등)
- 이슈: 발견된 문제점 또는 미완성 부분
- 기술스택: 사용된 라이브러리/프레임워크

[출력 형식]
{"category":"{{CATEGORY}}","files":[
  {"path":"...","role":"...","deps":["..."],"pattern":"...","issues":"...","stack":"..."},
  ...
]}
```

### 프롬프트 1-3: 아키텍처 종합 (Planner 역할)

```
너는 아키텍처 분석 Planner다.
아래 카테고리별 분석 결과를 종합하여 프로젝트 전체 아키텍처를 정리하라.

[분석 결과]
{{ALL_CATEGORY_RESULTS}}

[출력 항목]
1. 아키텍처 유형 (모놀리식/마이크로서비스/JAMstack 등)
2. 기술 스택 요약 (백엔드/프론트엔드/DB/인프라)
3. 데이터 흐름 (사용자요청 → 처리 → 응답 경로)
4. 현재 완성도 (0~100%)
5. 핵심 미완성 영역 (최대 5개)
6. 잠재적 위험 요소 (최대 3개)

[출력 형식]
마크다운. 각 항목을 ## 헤딩으로 구분.
다이어그램은 ASCII 또는 mermaid 코드블록 사용.
```

### STAGE 1 체크포인트 생성

```
위 분석 결과를 STAGE_1_COMPLETE.md 파일로 저장하라.
파일 상단에 아래 메타 정보를 포함:

---
stage: 1
status: complete
date: {{TODAY}}
project: {{PROJECT_NAME}}
next: STAGE 2 - 사용자 검토
---
```

---

## STAGE 2: 분석 보고서 생성 → 사용자 검토

### 프롬프트 2-1: 검토용 보고서 작성

```
너는 보고서 작성 Worker다.
STAGE_1_COMPLETE.md의 분석 결과를 바탕으로 사용자 검토용 보고서를 작성하라.

[규칙]
1. 기술 용어는 간결한 설명 병기.
2. 각 섹션 끝에 [확인 필요] 또는 [수정 필요] 태그 부착.
3. 사용자가 Y/N 또는 간단한 코멘트로 응답할 수 있게 구성.

[보고서 구조]
# 프로젝트 분석 보고서

## 1. 프로젝트 개요
- 프로젝트명:
- 목적:
- 기술스택:
→ [확인 필요] 위 내용이 맞습니까?

## 2. 현재 아키텍처
(다이어그램 포함)
→ [확인 필요] 이 구조가 의도한 설계입니까?

## 3. 완성된 부분
- 항목1 (완성도 N%)
- 항목2 (완성도 N%)
→ [확인 필요] 완성도 평가가 적절합니까?

## 4. 미완성 / 수정 필요 영역
- 영역1: 설명 → 제안: ...
- 영역2: 설명 → 제안: ...
→ [수정 필요] 우선순위를 정해주세요 (1,2,3...)

## 5. 제안 방향성
- 백엔드 방향: 
- 프론트엔드 방향: 
- DB 구조: 
- 배포 전략: 
→ [수정 필요] 동의하거나 수정 방향을 알려주세요.

## 6. 다음 단계 진행을 위해 필요한 정보
- 질문1:
- 질문2:
- 질문3:
```

---

## STAGE 3: 수정사항 반영 & 설계 확정

### 프롬프트 3-1: 사용자 피드백 반영

```
너는 설계 확정 Planner다.
사용자의 검토 피드백을 반영하여 최종 구현 계획을 수립하라.

[원본 보고서]
{{STAGE2_REPORT}}

[사용자 피드백]
{{USER_FEEDBACK}}

[규칙]
1. 피드백에서 변경된 사항만 추출하여 반영.
2. 변경 없는 항목은 "유지"로 표기.
3. 구현 계획은 STAGE 4~7에 매핑.

[출력 형식]
# 최종 구현 계획서

## 변경 사항 요약
| 항목 | 기존 | 변경 | 사유 |
|-----|------|------|-----|

## 백엔드 구현 계획 (STAGE 4)
- [ ] 태스크1: 설명 (예상 파일: N개)
- [ ] 태스크2: ...

## 프론트엔드 구현 계획 (STAGE 5)
- [ ] 태스크1: 설명
- [ ] 태스크2: ...

## 통합 계획 (STAGE 6)
- [ ] API 연동 포인트:
- [ ] 테스트 시나리오:

## 배포 계획 (STAGE 7)
- [ ] 환경 설정:
- [ ] 빌드 & 실행 방법:
```

### STAGE 3 체크포인트

```
최종 구현 계획서를 STAGE_3_COMPLETE.md로 저장하라.
이 파일이 이후 모든 STAGE의 기준 문서가 된다.
```

---

## STAGE 4: 백엔드 구축

### 프롬프트 4-0: 백엔드 시작 컨텍스트 로딩

> ⚠️ 중단 후 재개 시 이 프롬프트부터 사용

```
너는 백엔드 구축 Worker다.
아래 파일을 읽고 현재 상태를 파악한 후 작업을 시작하라.

[필수 읽기 파일]
1. STAGE_3_COMPLETE.md → 구현 계획서
2. 기존 백엔드 코드 진입점 파일

[규칙]
1. 계획서의 "백엔드 구현 계획" 섹션의 태스크를 순서대로 수행.
2. 한 번에 1개 태스크씩 완료 후 결과 보고.
3. 기존 코드가 있으면 최대한 재사용. 새로 작성은 최소화.
4. 각 태스크 완료 후 아래 형식으로 보고.

[보고 형식]
## 태스크 N 완료
- 작업: 
- 생성/수정 파일: [파일 목록]
- 다음 태스크: N+1
- 이슈: (있으면 기술, 없으면 "없음")
```

### 프롬프트 4-1: API 엔드포인트 구현

```
STAGE_3_COMPLETE.md의 백엔드 태스크 목록에 따라
API 엔드포인트를 구현하라.

[규칙]
1. 기존 라우터 파일이 있으면 그 패턴을 따라라.
2. 없으면 RESTful 컨벤션 적용.
3. 각 엔드포인트에 에러 핸들링 포함.
4. 주석은 한줄 요약만. 장문 주석 금지.
5. 완료 후 엔드포인트 목록표 출력.

[출력]
| Method | Path | 설명 | 상태 |
|--------|------|------|------|
```

### 프롬프트 4-2: 데이터 모델 / DB 구현

```
STAGE_3_COMPLETE.md 기준으로 데이터 모델을 구현하라.

[규칙]
1. 기존 스키마가 있으면 마이그레이션으로 처리.
2. 새로 만들면 기존 코드의 ORM/DB 라이브러리 사용.
3. 관계(relation) 정의 포함.
4. 시드 데이터가 필요하면 seed 파일 생성.
5. 완료 후 테이블/컬렉션 목록 출력.
```

### 프롬프트 4-3: 비즈니스 로직 & 미들웨어

```
STAGE_3_COMPLETE.md의 비즈니스 로직 태스크를 구현하라.

[규칙]
1. 서비스 레이어 분리 (라우터에 로직 직접 작성 금지).
2. 인증/인가 미들웨어 필요 시 구현.
3. 입력 유효성 검사(validation) 포함.
4. 기존 코드의 패턴과 네이밍 컨벤션을 따라라.
```

### STAGE 4 체크포인트

```
백엔드 구축 완료 보고를 STAGE_4_COMPLETE.md로 저장.

[포함 내용]
- 구현된 API 목록
- DB 스키마 요약
- 서버 실행 방법
- 미완료 항목 (있으면)
```

---

## STAGE 5: 프론트엔드 구축

### 프롬프트 5-0: 프론트엔드 시작 컨텍스트 로딩

> ⚠️ 중단 후 재개 시 이 프롬프트부터 사용

```
너는 프론트엔드 구축 Worker다.
아래 파일을 읽고 현재 상태를 파악한 후 작업을 시작하라.

[필수 읽기 파일]
1. STAGE_3_COMPLETE.md → 구현 계획서
2. STAGE_4_COMPLETE.md → 백엔드 API 목록
3. 기존 프론트엔드 진입점 파일

[규칙]
1. 계획서의 "프론트엔드 구현 계획" 태스크를 순서대로 수행.
2. 백엔드 API 목록을 참조하여 데이터 연동 설계.
3. 기존 프론트엔드 코드가 있으면 패턴 유지.
4. 한 번에 1개 페이지/컴포넌트씩 완료 후 보고.

[보고 형식]
## 컴포넌트/페이지 N 완료
- 작업: 
- 생성/수정 파일: [파일 목록]
- 연동 API: [엔드포인트 목록]
- 다음: N+1
```

### 프롬프트 5-1: 페이지 & 라우팅 구현

```
프론트엔드 페이지 구조와 라우팅을 구현하라.

[규칙]
1. 기존 라우팅이 있으면 확장. 없으면 신규 생성.
2. 페이지별 레이아웃 컴포넌트 분리.
3. 인증 필요 페이지는 가드(guard) 적용.
4. 완료 후 페이지 맵 출력:

| 경로 | 페이지명 | 인증필요 | 연동API |
|------|---------|---------|---------|
```

### 프롬프트 5-2: UI 컴포넌트 구현

```
각 페이지의 UI 컴포넌트를 구현하라.

[규칙]
1. 재사용 컴포넌트는 components/ 폴더에 분리.
2. 기존 스타일링 방식 유지 (CSS모듈/Tailwind/styled 등).
3. 기존 코드에 스타일링이 없으면 Tailwind CSS 적용.
4. 반응형 최소 기준: 모바일(360px), 데스크탑(1024px).
5. 한 번에 3개 컴포넌트씩 구현. 완료 후 목록 출력.
```

### 프롬프트 5-3: API 연동 & 상태관리

```
프론트엔드-백엔드 API 연동을 구현하라.

[필수 참조]
STAGE_4_COMPLETE.md의 API 목록

[규칙]
1. API 호출 함수는 별도 파일(api/, services/)에 분리.
2. 기존 HTTP 클라이언트(axios, fetch 등) 패턴 따라라.
3. 로딩/에러/성공 상태 처리 포함.
4. 기존 상태관리(Redux, Zustand, Context 등)가 있으면 유지.
5. 완료 후 연동 매트릭스 출력:

| 페이지 | API 엔드포인트 | 메서드 | 상태관리 |
|-------|--------------|--------|---------|
```

### STAGE 5 체크포인트

```
프론트엔드 구축 완료 보고를 STAGE_5_COMPLETE.md로 저장.

[포함 내용]
- 구현된 페이지/컴포넌트 목록
- API 연동 현황
- 실행 방법 (dev 서버)
- 미완료 항목 (있으면)
```

---

## STAGE 6: 통합 & 테스트

### 프롬프트 6-1: 통합 검증 (Critic 역할)

```
너는 통합 검증 Critic이다.
백엔드와 프론트엔드를 통합 검증하라.

[필수 읽기]
1. STAGE_4_COMPLETE.md
2. STAGE_5_COMPLETE.md

[검증 항목]
1. API 연동 일치성: 프론트가 호출하는 API가 백엔드에 모두 존재하는가
2. 데이터 타입 일치: 요청/응답 형식이 양쪽에서 일치하는가
3. 인증 흐름: 로그인→토큰→인가가 일관성 있는가
4. 에러 처리: 백엔드 에러를 프론트가 적절히 처리하는가
5. 환경 설정: .env 변수가 양쪽에서 올바르게 참조되는가

[출력 형식]
{"checks":[
  {"item":"API 연동","status":"pass|fail","detail":"..."},
  ...
],
"overall":"pass|fail",
"fixes_needed":["수정1","수정2"]}
```

### 프롬프트 6-2: 불일치 수정 (Worker 역할)

```
Critic의 검증 결과에서 fail 항목을 수정하라.

[검증 결과]
{{CRITIC_RESULT}}

[규칙]
1. fail 항목만 수정. pass 항목은 건드리지 마라.
2. 수정 후 해당 항목 재검증.
3. 수정 파일마다 변경 내용 1줄 요약.

[출력]
| 파일 | 변경 내용 | 검증결과 |
|------|----------|---------|
```

### 프롬프트 6-3: 실행 테스트

```
프로젝트를 실제 실행하여 동작을 확인하라.

[수행 절차]
1. 의존성 설치 (npm install / pip install 등)
2. DB 초기화 (마이그레이션, 시드)
3. 백엔드 서버 시작
4. 프론트엔드 dev 서버 시작
5. 주요 기능 동작 확인

[보고]
| 단계 | 명령어 | 결과 | 이슈 |
|------|--------|------|------|

실행 중 에러 발생 시:
1. 에러 메시지 전문 기록
2. 원인 분석
3. 수정 후 재실행
```

### STAGE 6 체크포인트

```
통합 테스트 결과를 STAGE_6_COMPLETE.md로 저장.

[포함 내용]
- 검증 항목별 결과
- 수정된 파일 목록
- 실행 테스트 결과
- 잔여 이슈 (있으면)
```

---

## STAGE 7: 배포 준비 & 최종 점검

### 프롬프트 7-1: 배포 설정

```
프로젝트를 배포 가능 상태로 준비하라.

[규칙]
1. .env.example 파일 생성 (실제 값 제외, 키만)
2. 빌드 명령어 확인 및 테스트
3. 프로덕션용 설정 분리 (DB URL, API URL 등)
4. CORS, 보안 헤더 설정 확인
5. README.md 작성:

[README 필수 내용]
- 프로젝트 소개 (3줄 이내)
- 기술 스택
- 설치 방법 (복사-붙여넣기 가능한 명령어)
- 환경 변수 설명
- 실행 방법
- 빌드 방법
- 폴더 구조 (1단계)
```

### 프롬프트 7-2: 최종 점검 (Critic 역할)

```
최종 산출물을 점검하라.

[점검 항목]
1. 모든 STAGE 체크포인트 파일 존재 확인
2. package.json / requirements.txt 의존성 누락 확인
3. 하드코딩된 비밀값 검색 (API키, 비밀번호 등)
4. console.log / print 디버그 코드 제거 확인
5. .gitignore 설정 확인
6. 빌드 성공 확인

[출력]
# 최종 점검 결과
| 항목 | 상태 | 비고 |
|------|------|------|
...
전체 판정: ✅ 배포 가능 / ⚠️ 수정 필요
```

### STAGE 7 체크포인트 (최종)

```
STAGE_7_COMPLETE.md로 최종 보고서 저장.

[포함 내용]
- 프로젝트 최종 구조
- 실행/빌드 방법
- 잔여 이슈 및 향후 개선사항
- 전체 작업 소요 토큰 추정
```

---

## 🔄 중단 복구 프롬프트

> 작업이 중간에 끊겼을 때 아래 프롬프트로 재개

```
너는 프로젝트 구축 오케스트레이터다.
작업이 중단되었다. 현재 상태를 파악하고 재개하라.

[수행 절차]
1. 프로젝트 루트에서 STAGE_*_COMPLETE.md 파일을 검색하라.
2. 가장 높은 번호의 체크포인트를 읽어라.
3. 해당 STAGE의 다음 STAGE부터 작업을 재개하라.
4. 체크포인트가 없으면 STAGE 0부터 시작하라.

[보고]
- 발견된 체크포인트: STAGE N
- 마지막 완료 내용: (체크포인트 요약)
- 재개 지점: STAGE N+1
- 재개합니다. (자동 진행)
```

---

## 📏 토큰 절약 규칙 (모든 STAGE 공통)

이 규칙을 모든 프롬프트 실행 시 적용:

```
[공통 토큰 절약 규칙 - 모든 응답에 적용]
1. 인사·면책 문구 금지 ("안녕하세요", "도움이 되었길" 등)
2. 코드 설명은 주석 1줄로 대체. 별도 설명문 금지.
3. 동일 정보 반복 금지. 이전 STAGE 파일 참조로 대체.
4. 파일 전체 내용 출력 금지. 변경 부분만 diff 형식으로.
5. 결과 보고는 테이블 형식 사용. 서술형 금지.
6. 한 태스크 완료 전 다음 태스크 시작 금지.
7. 불확실한 사항은 가정하지 말고 질문하라.
```

---

## 📎 Claude Skill 활용 가이드

| 상황 | 사용할 Skill | 경로 |
|------|-------------|------|
| 분석 보고서를 Word로 작성 | docx | `/mnt/skills/public/docx/SKILL.md` |
| PDF 보고서 생성 | pdf | `/mnt/skills/public/pdf/SKILL.md` |
| 프론트엔드 UI 설계 | frontend-design | `/mnt/skills/public/frontend-design/SKILL.md` |
| 엑셀로 데이터 정리 | xlsx | `/mnt/skills/public/xlsx/SKILL.md` |
| MCP 서버 구축 필요 시 | mcp-builder | `/mnt/skills/examples/mcp-builder/SKILL.md` |

**Skill 호출 방법 (Claude Code에서):**
```bash
# 파일 생성 작업 전에 항상 스킬 파일을 먼저 읽도록 지시
"작업 전에 /mnt/skills/public/{{skill}}/SKILL.md를 읽고 그 지침을 따라라."
```

---

## 🗺️ 빠른 참조: STAGE별 입력 프롬프트

| STAGE | 시작 명령어 | 예상 토큰 |
|-------|-----------|----------|
| 0 | `"0단계 시작: 압축 해제"` | ~200 |
| 1 | `"1단계 시작: 코드 분석"` | ~1,500 |
| 2 | `"2단계 시작: 보고서 생성"` | ~800 |
| 3 | `"3단계 시작: 피드백 반영" + [피드백 내용]` | ~600 |
| 4 | `"4단계 시작: 백엔드 구축"` | ~2,000 |
| 5 | `"5단계 시작: 프론트엔드 구축"` | ~2,500 |
| 6 | `"6단계 시작: 통합 테스트"` | ~1,000 |
| 7 | `"7단계 시작: 배포 준비"` | ~500 |
| 복구 | `"작업 재개"` | ~300 |

**총 예상 토큰: ~9,400** (전체 파이프라인, 중간 규모 프로젝트 기준)

---

*v1.0 — 이 문서의 프롬프트를 Claude Code 터미널에 순서대로 입력하여 사용하세요.*
