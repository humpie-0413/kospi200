# 🎯 사용자 가이드 — KOSPI200 에이전트 팀 운영 매뉴얼

---

## 📌 이 문서는 뭔가요?

이 문서는 **Git Bash에서 Claude Code를 열어 한 마디만 입력하면** 에이전트 팀이
자동으로 작업하는 시스템의 사용 설명서입니다.

**당신이 할 일**: 각 단계마다 Git Bash를 열고, 정해진 한 줄을 입력하고, 결과를 확인하는 것.

---

## 🔧 최초 1회 세팅

### 1. 프로젝트 폴더 확인

```
C:\0_project\kospi200\
├── CLAUDE.md              ← 이미 있음 (자동 로딩됨)
├── .claude/rules/         ← 이미 있음 (자동 로딩됨)
├── stages/                ← 이미 있음 (단계별 지시서)
├── checkpoints/           ← 이미 있음 (빈 폴더)
├── before/                ← ⚡ 여기에 기존 코드를 넣으세요
└── USER-GUIDE.md          ← 이 문서
```

### 2. 기존 코드 배치

기존 프로젝트 코드를 `before/` 폴더에 넣어주세요.
- 압축 파일(.zip)이면 그대로 넣어도 됩니다 (자동 해제됨)
- 폴더째 넣어도 됩니다

### 3. Claude Code 실행

Git Bash를 열고:

```bash
cd /c/0_project/kospi200
claude
```

이것으로 세팅 완료입니다. `CLAUDE.md`가 자동으로 로딩됩니다.

---

## 🚀 단계별 실행 가이드

### 각 단계의 흐름

```
Git Bash 열기 → cd /c/0_project/kospi200 → claude → 한 마디 입력 → 결과 확인
```

> 💡 **핵심**: 매번 새 세션에서도 `CLAUDE.md`가 자동 로딩되므로
> 에이전트 설정, 토큰 규칙 등을 다시 입력할 필요가 없습니다.

---

### STAGE 0 — 환경 확인

**입력할 한 마디:**
```
0단계 시작
```

**Claude가 하는 일:**
- `before/` 폴더 확인 및 압축 해제
- 프로젝트 구조 파악
- 언어/프레임워크 감지

**당신이 확인할 것:**
- 파일 목록이 맞는지
- 감지된 기술스택이 맞는지

**완료 표시:** `checkpoints/STAGE_0_COMPLETE.md` 생성됨

---

### STAGE 1 — 코드 분석

**입력할 한 마디:**
```
1단계 시작
```

**Claude가 하는 일:**
- 카테고리별로 파일 분류 (진입점, API, 모델, 로직, 프론트, 설정)
- 각 파일 심층 분석
- 전체 아키텍처 종합

**소요 시간:** 프로젝트 규모에 따라 2~10분

**⚠️ 중간에 끊기면:**
새 Git Bash 열고 → `claude` → `재개` 입력

**완료 표시:** `checkpoints/STAGE_1_COMPLETE.md` 생성됨

---

### STAGE 2 — 분석 보고서 검토

**입력할 한 마디:**
```
2단계 시작
```

**Claude가 하는 일:**
- 분석 결과를 검토용 보고서로 정리
- 각 항목에 `[확인]` / `[수정필요]` 태그 부착

**당신이 할 일 (⭐ 중요):**
보고서를 읽고 피드백을 입력하세요. 예시:

```
1번 맞음. 2번 맞음.
4번 수정: 우선순위는 인증>대시보드>데이터수집 순서로.
5번 수정: 프론트는 React 대신 Next.js로 변경.
DB는 PostgreSQL 유지.
추가: 실시간 데이터 갱신 기능 필요.
```

> 간단하게 써도 됩니다. Claude가 알아서 해석합니다.

**완료 표시:** `docs/analysis-report.md` 생성됨

---

### STAGE 3 — 설계 확정

**입력할 한 마디:**
```
3단계 시작
```

> ⚠️ STAGE 2에서 피드백을 이미 입력한 같은 세션이라면 자동으로 진행됩니다.
> 새 세션이라면 피드백 내용을 다시 간단히 알려주세요:

```
3단계 시작. 피드백: 프론트 Next.js, 인증 우선, 실시간 데이터 추가.
```

**Claude가 하는 일:**
- 피드백 반영하여 최종 구현 계획서 작성
- STAGE 4~7의 구체적 태스크 목록 생성

**당신이 확인할 것:**
- 태스크 목록이 빠짐없는지
- 기술스택 선택이 맞는지

**완료 표시:** `checkpoints/STAGE_3_COMPLETE.md` 생성됨

---

### STAGE 4 — 백엔드 구축

**입력할 한 마디:**
```
4단계 시작
```

**Claude가 하는 일:**
- 계획서의 백엔드 태스크를 순서대로 실행
- API 엔드포인트, DB 모델, 비즈니스 로직 구현
- 태스크마다 완료 보고

**소요 시간:** 규모에 따라 5~20분

**⚠️ 컨텍스트가 길어지면:**
Claude가 `/compact`을 제안하거나, 새 세션에서 `재개` 입력

**완료 표시:** `checkpoints/STAGE_4_COMPLETE.md` 생성됨

---

### STAGE 5 — 프론트엔드 구축

**입력할 한 마디:**
```
5단계 시작
```

**Claude가 하는 일:**
- 계획서의 프론트엔드 태스크를 순서대로 실행
- 페이지, 컴포넌트, API 연동 구현
- 백엔드 API 목록 참조하여 데이터 연결

**소요 시간:** 규모에 따라 5~20분

**완료 표시:** `checkpoints/STAGE_5_COMPLETE.md` 생성됨

---

### STAGE 6 — 통합 테스트

**입력할 한 마디:**
```
6단계 시작
```

**Claude가 하는 일:**
1. 백엔드↔프론트엔드 연동 불일치 검사
2. 문제 발견 시 자동 수정
3. 실제 서버 실행 테스트

**당신이 확인할 것:**
- 테스트 결과 표
- 잔여 이슈 목록

**완료 표시:** `checkpoints/STAGE_6_COMPLETE.md` 생성됨

---

### STAGE 7 — 배포 준비

**입력할 한 마디:**
```
7단계 시작
```

**Claude가 하는 일:**
- .env.example, README.md 생성
- 빌드 테스트
- 보안 점검 (하드코딩 비밀값, 디버그 코드)
- 최종 판정: ✅ 배포 가능 / ⚠️ 수정 필요

**완료 표시:** `checkpoints/STAGE_7_COMPLETE.md` 생성됨 → **프로젝트 완료!**

---

## 🔄 중단 & 복구

### 작업 중 끊겼을 때

새 Git Bash를 열고:

```bash
cd /c/0_project/kospi200
claude
```

그리고 입력:

```
재개
```

Claude가 `checkpoints/` 폴더를 확인하고 마지막 완료 지점 다음부터 자동 재개합니다.

### 특정 단계를 다시 하고 싶을 때

```
3단계 다시 시작
```

해당 단계의 체크포인트를 덮어쓰고 재실행합니다.

---

## 📋 한눈에 보는 명령어 표

| 상황 | 입력할 말 |
|------|----------|
| 환경 확인 | `0단계 시작` |
| 코드 분석 | `1단계 시작` |
| 보고서 검토 | `2단계 시작` |
| 피드백 반영 | `3단계 시작` |
| 백엔드 구축 | `4단계 시작` |
| 프론트 구축 | `5단계 시작` |
| 통합 테스트 | `6단계 시작` |
| 배포 준비 | `7단계 시작` |
| 중단 복구 | `재개` |
| 단계 재실행 | `N단계 다시 시작` |
| 현재 상태 확인 | `현재 상태` |

---

## ⚠️ 주의사항

1. **`before/` 폴더는 수정하지 마세요** — 원본 코드 보존용입니다
2. **CLAUDE.md를 직접 수정하지 마세요** — 시스템이 깨질 수 있습니다
3. **단계를 건너뛰지 마세요** — 각 단계는 이전 체크포인트에 의존합니다
4. **피드백은 구체적으로** — "좋아요" 보다 "인증은 JWT로, DB는 PostgreSQL" 같이
5. **새 세션마다 `claude` 명령어로 시작** — CLAUDE.md가 자동 로딩됩니다

---

## 🗂️ 파일 구조 설명

```
kospi200/
├── CLAUDE.md              # [자동] Claude Code가 매 세션 읽는 시스템 설정
├── .claude/
│   └── rules/
│       └── token-rules.md # [자동] 토큰 절약 규칙 (매 세션 자동 로딩)
├── stages/
│   ├── stage0.md          # [참조] 0단계 상세 지시
│   ├── stage1.md          # [참조] 1단계 상세 지시
│   ├── stage2.md          # [참조] 2단계 상세 지시
│   ├── stage3.md          # [참조] 3단계 상세 지시
│   ├── stage4.md          # [참조] 4단계 상세 지시
│   ├── stage5.md          # [참조] 5단계 상세 지시
│   ├── stage6.md          # [참조] 6단계 상세 지시
│   ├── stage7.md          # [참조] 7단계 상세 지시
│   └── resume.md          # [참조] 중단 복구 지시
├── checkpoints/           # [자동생성] 단계별 완료 기록
├── before/                # [사용자] 기존 프로젝트 코드
├── src/                   # [자동생성] 새로 구축되는 코드
├── docs/                  # [자동생성] 분석 보고서, 설계 문서
└── USER-GUIDE.md          # 이 문서
```

| 태그 | 의미 |
|------|------|
| [자동] | Claude Code가 자동으로 읽거나 생성 |
| [참조] | Claude가 필요할 때 읽는 지시 파일 |
| [사용자] | 사용자가 직접 넣는 파일 |
| [자동생성] | 작업 진행 중 생성되는 파일/폴더 |
